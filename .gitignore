# === Node / Next.js ===
node_modules/
.next/
out/
.turbo/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# === Python ===
__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.*
!.env.example
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# === Terraform ===
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
override.tf
override.tf.json
tfplan
tfplan*
*.tfplan

# === Secrets / credentials (NEVER commit) ===
*.pem
*.key
aws.json
credentials
.aws/

# === Build / artifacts ===
dist/
build/
*.log
tmp/
.DS_Store
Thumbs.db

# === Editor ===
.vscode/
.idea/
*.swp

# === Data / production logs (NEVER commit prod data to this public repo) ===
*_log.csv
3654414_log.csv
# Source/test media (multi-GB); keep locally, never in git. Committed test
# fixtures use *.sample.csv (small, checked in under tests/fixtures/).
*.mp4
*.mov
*.mkv
